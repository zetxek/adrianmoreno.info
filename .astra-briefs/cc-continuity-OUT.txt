I've now fully mapped the actual target codebase and re-verified the baseline. Here's where things stand before I start cutting code.

## Where the work actually lives

Your literal cwd (`adrianmoreno.info-cont`) is a scratch folder containing only the spec files and a splice helper — no site code. The real target is `/home/zetxek/Projects/adrianmoreno.info-race` (branch `feature/engineered-race-game-mode`), confirmed by exact match: `zones.js` is 2014 lines/55 declarations, `enhancements.html` has 23 `race-athlete` references, and its own `.astra-briefs/astra-continuity/00-continuity-spec.md` / `astra-gamify/00-gamify-spec.md` are byte-identical to the ones you pointed me at. I'll do all work there. Measured baseline, freshly run: `hugo --gc --minify` clean, `node --test tests/unit/` **71/71 pass**, unit test's `EXPECTED_PER_ZONE` matches the spec's "current inventory" table exactly (4,770 tri / 506 instances).

I mapped the whole race stack: `main.js` (per-zone camera recipes + dissolve blend + inspection raycasting + T1/T2 scroll-tied animation — all things the continuity spec requires removing/replacing), `zones.js` (all 55 named functions confirmed present, including several already-unused "preserved" builders like `horreoRow`, `landmarkTower`, `canalHouses`, `archBridge` — dead but intact, exactly matching the guard's list), `atlas-motion.js` (the toy-atlas wake the continuity spec explicitly orders removed), and `assets/js/race/*` (a genuinely well-built controller that **already has a working, non-persisted passport** — explicit save toggle, count, links, reset, live-region announcements — which is a real head start on the gamify spec).

## Two things I need a decision on before cutting code

**1. A direct contradiction on `enhancements.html`, the same file the last attempt got burned on.** Your hard guard says its athlete-SVG pose markup "must NOT be reduced." But gamify spec §6.2 and rubric item 9, plus the numeric build gate in §9.4 ("Legacy athlete pose groups remaining: **0**"), explicitly require deleting all 84 pose elements and replacing them with a single 4-part boat glyph — and §8.3 of that same spec pre-computes this exact removal in its own DOM budget arithmetic. Both documents are marked binding; they cannot both be true of the same file. My read is that the hard guard is warning against *careless* collateral deletion (the last attempt's real sin was a reckless whole-file rewrite, not a considered, budgeted removal), and that the boat-glyph swap is a deliberate, spec-mandated fix for the *identical* bug the guard describes (blank/zero-rect marker on mobile) — so I'd implement the replacement surgically and account for every removed element, rather than silently obeying whichever instruction I like better. **Confirm that's the right call before I touch that file.**

**2. One acceptance criterion is not something I can execute.** Gamify §4.2 requires an unprompted 5-second-exposure test with 10 real first-time visitors (≥8/10 must notice the passport unprompted). I have no human subjects available. I'll build to every measurable bound around it (card position/size/contrast at 390×844, discoverability geometry, etc.) but the human study itself has to be run by you or a real tester — I'll flag it as outstanding rather than fabricate a result.

## Scope reality

Full literal compliance is a large build: a new Hermite-spline motion law with six-decimal seam equality, geometry surgery across every zone (bridge rise, water-slab removal, Copenhagen boat removal, berth platform, palette-role cleanup), a persistent vessel/water/wake system replacing the toggle-based dissolve, and the gamify DOM layer (discoverability card, lead-selector, finish brief, localStorage persistence, byte/DOM budgets) — plus rewriting the two existing E2E tests that currently assert the old pose-swapping and toy-atlas behavior. That's realistically many more edit/verify rounds. I'll proceed section by section, running unit/build checks as I go, and report real measured numbers at the end rather than a running narration.

**Before I start:** please confirm point 1 (boat glyph replaces the athlete, deliberately and surgically), and let me know if you want this done in one continuous push or checkpointed at a natural boundary (e.g., continuity/motion layer first, gamify layer second).
